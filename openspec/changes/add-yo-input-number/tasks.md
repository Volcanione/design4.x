# Tasks: add-yo-input-number

- [x] 新增 `YoInputNumber` wrapper。
- [x] 新增 `yo-input-number` 稳定根类名和 `yo-input-number--console` 主题类名。
- [x] 接入公共 `disabledVariant`。
- [x] 新增 `input-number/style/index.less`。
- [x] 新增 `index.zh-CN.md` 和 demo。
- [x] 设置 `YoInputNumber size="large"` 左右内边距为 `16px`。
- [x] 设置 `YoInputNumber size="large"` 字号保持 `14px`。
- [x] 明确 `controls` 用于控制右侧增减按钮显示与隐藏。
- [x] 同步全量导出、全局类型、resolver 和子路径 exports。
- [x] 同步按需样式复制脚本。
- [x] 同步预览路由生成标题。
- [x] 通过 `pnpm run typecheck`。
- [x] 通过 `pnpm run build`。
- [x] 通过 `git diff --check`。
